<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.smhrd.foodie.mapper.MemberMapper">
 	
 	<insert id="userJoin" parameterType="com.smhrd.foodie.model.Member">
        insert into member_info (mem_id, mem_pw, mem_name, mem_email, mem_phone, mem_addr)
        values (#{mem_id}, #{mem_pw}, #{mem_name}, #{mem_eamil}, #{mem_phone}, #{mem_addr})
    </insert>
    
    <delete id="delete" parameterType="com.smhrd.foodie.model.Member">
    	delete from member where id = #{mem_id}
    </delete>
    
    <select id="login" parameterType="com.smhrd.foodie.model.Member"
    resultType="com.smhrd.foodie.model.Member">
    	select * from member_info where id = #{id} and pw = #{pw}
    </select>
 	
 </mapper>