<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 
 <!-- 3. 찾은 후 해당 sql문 실행
 4. SQL문 실행 결과 (영향을 받은 행 수)를 다시 반환-->
 <mapper namespace="com.smhrd.foodie.mapper.MemberMapper">
    
    <!-- 회원가입 -->
    <insert id="join" parameterType="Map">
        insert into member_info (mem_id, mem_pw, mem_name, mem_email, mem_phone, mem_addr)
        values (#{mem_id}, #{mem_pw}, #{mem_name}, #{mem_email}, #{mem_phone}, #{mem_addr})
    </insert>
    
    <!-- 회원가입 시 알러지 정보 추가 -->
    <insert id="allergy" parameterType="com.smhrd.foodie.model.MemberAllergy">
        insert into member_allergy_info (mem_id, allergy_idx) values (#{mem_id}, #{allergy_idx})
    </insert>
    
        <!-- 회원가입 시 비선호 식재료 정보 추가 -->
    <insert id="dislike" parameterType="com.smhrd.foodie.model.MemberDislike">
        insert into member_dislike_info (mem_id, dislike_idx) values (#{mem_id}, #{dislike_idx})
    </insert>
    
   
    
    <!-- 로그인 -->
    <select id="loginCheck" parameterType="com.smhrd.foodie.model.Member" resultType="com.smhrd.foodie.model.Member">
       select mem_id, mem_pw from member_info where mem_id = #{mem_id} and mem_pw = #{mem_pw}
    </select>
    
    <!-- 회원가입했을 때 입력받은 알러지 정보를 마이페이지 체크박스에 반영 -->
    <!-- 조회된 값만 리턴해줌 -->
 	<select id="allergyMypage" parameterType="com.smhrd.foodie.model.Member" resultType="com.smhrd.foodie.model.MemberAllergy">
    	select * from member_allergy_info where mem_id = #{mem_id}
    </select>
    
    
    
</mapper>